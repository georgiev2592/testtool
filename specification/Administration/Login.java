package Administration;

/**
 * The Login class allows a user to log in the TestTool.
 */
public abstract class Login {
   /**
    * A method that takes a User object as a parameter and logs in the user to the system.
    */
   abstract void login(User a);
}
